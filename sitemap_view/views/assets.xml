<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- res.partner sitemap view -->
    <record id="res_partner_view_sitemap" model="ir.ui.view">
        <field name="name">res.partner.view.sitemap</field>
        <field name="model">res.partner</field>
        <field name="arch" type="xml">
            <sitemap decoration-danger="phone==False">
                <field name="display_name" string="Nombre"/>
                <field name="function" invisible="1"/>
                <field name="phone" optional="show"/>
                <field name="email" optional="show"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <field name="city" optional="hide"/>
                <field name="state_id" optional="hide"/>
                <field name="country_id" optional="hide"/>
                <field name="vat" optional="hide"/>
                <field name="user_id" invisible="1"/>
                <field name="is_company" invisible="1"/>
                <field name="parent_id" invisible="1"/>
                <field name="child_ids" invisible="1"/>
                <field name="active" invisible="1"/>
            </sitemap>
        </field>
    </record>
</odoo>
